name: dart_debounce_throttle
description: Debounce, throttle, and rate limiting for Pure Dart. Zero dependencies, async support, batch processing, and distributed rate limiting.
version: 2.4.3
homepage: https://github.com/brewkits/flutter_debounce_throttle
repository: https://github.com/brewkits/flutter_debounce_throttle/tree/main/packages/dart_debounce_throttle
issue_tracker: https://github.com/brewkits/flutter_debounce_throttle/issues

topics:
  - debounce
  - throttle
  - rate-limiting
  - async
  - dart

environment:
  sdk: ^3.0.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
