name: dart_debounce_throttle
description: Pure Dart debounce/throttle for servers (Serverpod, Dart Frog). Distributed Redis rate limiting, batch operations, 360+ tests. Better than easy_debounce.
version: 2.4.5
homepage: https://github.com/brewkits/flutter_debounce_throttle
repository: https://github.com/brewkits/flutter_debounce_throttle/tree/main/packages/dart_debounce_throttle
issue_tracker: https://github.com/brewkits/flutter_debounce_throttle/issues

topics:
  - debounce
  - throttle
  - async
  - server
  - redis

environment:
  sdk: ^3.0.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
