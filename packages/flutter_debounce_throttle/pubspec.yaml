name: flutter_debounce_throttle
description: Production-ready debounce, throttle, and rate limiting for Flutter. Prevent double-clicks, API spam, and memory leaks with auto lifecycle management.
version: 2.4.2
homepage: https://github.com/brewkits/flutter_debounce_throttle
repository: https://github.com/brewkits/flutter_debounce_throttle
issue_tracker: https://github.com/brewkits/flutter_debounce_throttle/issues

topics:
  - debounce
  - throttle
  - rate-limiting
  - ui
  - production

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dart_debounce_throttle: ^2.4.2
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

flutter:
