name: flutter_debounce_throttle_hooks
description: Flutter Hooks for flutter_debounce_throttle. Memory-safe debounce/throttle with auto-dispose. Better than easy_debounce. React-style hooks for Flutter.
version: 2.4.5
homepage: https://github.com/brewkits/flutter_debounce_throttle
repository: https://github.com/brewkits/flutter_debounce_throttle
issue_tracker: https://github.com/brewkits/flutter_debounce_throttle/issues

topics:
  - debounce
  - throttle
  - flutter-hooks
  - hooks
  - react

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3
  flutter_debounce_throttle: ^2.4.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

flutter:
