name: flutter_debounce_throttle_core
description: >-
  Pure Dart core for flutter_debounce_throttle. Zero external dependencies.
  Debounce, throttle, and rate limit with no Flutter dependency.
  Works on Mobile, Web, Desktop, Server, and CLI.
version: 1.0.0
homepage: https://brewkits.dev
repository: https://github.com/brewkits/flutter_debounce_throttle
issue_tracker: https://github.com/brewkits/flutter_debounce_throttle/issues

topics:
  - debounce
  - throttle
  - rate-limiting
  - async
  - dart

environment:
  sdk: ^3.0.0

dependencies:
  meta: ^1.17.0

dev_dependencies:
  test: ^1.29.0
  lints: ^6.0.0
